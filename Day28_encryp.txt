npm install hono jsonwebtoken bcrypt
npm install -D typescript ts-node @types/node @types/jsonwebtoken @types/bcrypt

itsec_hono_2509 에 위 2개 타이핑 설치

테이블플러스
-t_user - open structure - SQL - 전체 지우고 복붙 - 하나 하나마다 - 마우스 올리고 Run Current - 총 2개
ALTER TABLE t_user
ADD COLUMN real_name VARCHAR DEFAULT '';

ALTER TABLE t_user
ADD COLUMN profile_url VARCHAR DEFAULT '';


로그인 방식 크게 2가지
1. sever side
- 서버에서 웹까지 합침
- 세션으로 로그인
- Http 요청 - connetionless(기억하지 못함)-JWT 매번 인증(but JWT 털리면 해킹 큰일 남)
- JWT SECRET도 털리면 안됨 위조가 난리남


2. c/s 분리 - 요즘 많이 사용하는 방식


typeorm-model-generator -h ep-shy-cloud-ae0v32b0-pooler.c-2.us-east-2.aws.neon.tech -d neondb -u neondb_owner -x npg_bj8OZeBaLE9y -e postgres -o ./src --ssl
모델 제네레이터 입력