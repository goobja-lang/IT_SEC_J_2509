조립을 하는 것이 쉽지가 않다.

w3school - React Router - npm install react-router-dom 복사 - 터미널 붙여넣기 하면 설치

src - 뉴폴더 Pages - App.tsx 복사 - Pages에 붙여넣고 Calc.tsx로 붙여넣기
Pages가 src 하부에 있어야 함
Git 올라와져있는 것 확인하고 복붙



-------------------------------------------
예제 - App.tsx 에 붙여넣기
import { useEffect, useState } from "react";
import reactLogo from "./assets/react.svg";
import viteLogo from "/vite.svg";
import "./App.css";
import { BrowserRouter, Route, Routes } from "react-router-dom";
import Calc from "./Pages/Calc";

function App() {
  return (
    <>
      <BrowserRouter>
        <Routes>
          <Route path="/" element={<Calc />} />
        </Routes>
      </BrowserRouter>
    </>
  );
}

export default App;



